import { PeleTitlePipe } from './pele-title.pipe';

describe('PeleTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PeleTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
